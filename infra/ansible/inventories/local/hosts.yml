---
# ── Local k3d cluster inventory ──
# K3s is already installed inside k3d containers.
# We manage the cluster via kubectl on the host.
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: /usr/bin/python3
  vars:
    env_type: local
