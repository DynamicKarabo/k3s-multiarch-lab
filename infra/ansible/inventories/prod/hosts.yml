---
# ── Production cluster inventory (Hetzner + Oracle) ──
all:
  children:
    k3s_server:
      hosts:
        hetzner-vps:
          ansible_host: 178.105.76.236
          ansible_user: root
          ansible_ssh_private_key_file: ~/.ssh/id_ed25519
          node_role: server
          node_type: amd64
    k3s_agent:
      hosts:
        oracle-arm:
          ansible_host: ""  # set after OCI provisioning
          ansible_user: ubuntu
          ansible_ssh_private_key_file: ~/.ssh/id_ed25519
          node_role: agent
          node_type: arm64
  vars:
    env_type: prod
